# Test if XR is ready since the resources (buckets, policies, user) are supplied via --observed-resources
# Test if XR creates expected resource manifests (buckets, policies, user)
apiVersion: pkg.internal/v1beta1
kind: XStorage
metadata:
  labels:
    crossplane.io/claim-name: alice-002
    crossplane.io/claim-namespace: default
  name: alice-002
spec:
  owner: alice-002
  buckets:
  - bucketName: alice-002
    discoverable: false
  - bucketName: alice-shared-002
    discoverable: true
