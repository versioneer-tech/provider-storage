apiVersion: minio.crossplane.io/v1
kind: ProviderConfig
metadata:
  name: provider-minio
spec:
  credentials:
    apiSecretRef:
      name: provider-minio-secret
      namespace: minio
    source: InjectedIdentity
  minioURL: "http://myminio-hl.minio-test.svc.cluster.local:9000/"
